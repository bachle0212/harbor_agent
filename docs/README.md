# Assistant screenshot

`assistant-youtube.png` — Gemini File Search answer to **How do I add a YouTube video?** with an `Article URL:` citation.

The HTML source is `assistant-youtube.html` (regenerate with headless Chrome if needed).
